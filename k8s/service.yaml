apiVersion: v1
kind: Service
metadata:
  name: realestateapp-service
  labels:
    app: realestateapp
spec:
  type: LoadBalancer
  sessionAffinity: ClientIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: realestateapp